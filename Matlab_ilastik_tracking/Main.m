
%% Based on information from first run-through of Track Cells, user should edit connected cells that failed to be split properly

% Do this by side-to-side interactive display of:
% Initial tracking run in RGB
% Raw images
% Object Predictions. This one should be editable via a paint bar.

% imstack = readstack(FIXED_STACK);

TrackCells